一、概念
单例模式（singleton Pattern）
是JAVA中最简单的设计模式之一，这种类型的设计模式属于创建型模式，它提供了一种创建对象的最佳方式。

这种模式涉及到一个单一类，该类负责创建自己的对象，同时确保只有单个对象被创建。
这个类提供了一种访问其唯一的对象的方式，可以直接访问，不需要实例化该对象。

注：
1.单例类只能有一个实例
2.单例类必须自己创建自己唯一的实例
3.单例类必须给其他所有对象提供这一实例

何时使用：当你想控制实例数目，节省系统资源的时候

如何解决：
1.判断